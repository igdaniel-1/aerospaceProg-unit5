DATE:
01152026

UPDATES:
I spoke with some of the Physics dept at [university], they gave me some pointers I will now employ.

ISSUES:
Installs were showing up as local instead of in VM.

ATTEMPTS:
Used Anaconda to create a new VM. using Jupyter notebook, I was able to execute the PyAstronomy install in the new Anaconda VM's directory. 
I was also able to execute a .ipynb file from one of the prof's.
Next, I will convert part of my finalProject.py file into .ipynb for testing.
Then, I will attempt to run the .py file in the Anaconda VM.